<div class="card">
  <div class="card-header">
    <h3>Edit Gig</h3>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <%= render 'form', gig: @gig %>
    </blockquote>
  </div>
</div>

<br/>
<%= link_to 'Show', @gig, class:"btn btn-outline-info my-2 my-sm-0" %> |
<%= link_to 'Back', gigs_path, class:"btn btn-outline-info my-2 my-sm-0" %>
